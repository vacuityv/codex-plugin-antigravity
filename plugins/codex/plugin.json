{
  "name": "codex",
  "version": "0.1.0",
  "description": "Use OpenAI Codex from inside Google Antigravity. Adds a `codex` skill so the Antigravity agent can shell out to the local `codex` CLI (`codex exec`) for read-only code review and task delegation, plus a background-job runner (status / result / cancel)."
}
